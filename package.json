{
  "name": "@kcbb/root",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "test:coverage": "pnpm -r test:coverage",
    "push": "pnpm -r build && node scripts/push.js"
  }
}
